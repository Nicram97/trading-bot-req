// import { AnalyzeResult } from 'src/types/analyze-result';
// import { Trade } from 'src/types/trade.interface';
// import { ExchangeStrategy } from '../types/exchange.strategy';

// export class UpDownStrategy implements ExchangeStrategy {
//   /**
//    *
//    * @param trades Analyze the fetched data to identify changes in cryptocurrency prices,
//    *  such as increases or decreases, over the specified time period.
//    */
// //   execute(trades: Trade[]): Promise<AnalyzeResult> {
//     // lets assume that start/end time are the same for extracting from binance exchange and for analyze
//     // binance trades are provided from earliest for eldest so they are already "sorded"
//     // what we require: basically take 1st and last check their value and provide result
//     // also it would be good to provide amount off assets available for example if we have 1BTC in our "wallet"
//     // we can check price between start and end and know if our asset value increased or decreased
// //   }
// }
