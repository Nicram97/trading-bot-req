// In future API url could be provided into request to "createBot" so it would be configurable but for now it is only an analyzer
export interface Config {
  BINANCE_API_URL: string;
  API_KEY: string;
}
